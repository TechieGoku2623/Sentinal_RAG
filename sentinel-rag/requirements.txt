# --- Core agent / LLM orchestration ---
# NOTE: langchain-core is pinned to >=0.2.22 because langgraph 0.1.19 requires
# it (langchain-core==0.2.10 is incompatible and breaks resolution).
langgraph==0.1.19
langchain==0.2.6
langchain-groq==0.1.6
langchain-community==0.2.6
langchain-core==0.2.28
langsmith==0.1.77

# --- Vector store + embeddings ---
chromadb==0.5.3
sentence-transformers==3.0.1

# --- UI ---
streamlit==1.35.0

# --- API / platform ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
python-multipart==0.0.20
httpx==0.28.1

# --- Document loading / config ---
python-dotenv==1.0.1
pypdf2==3.0.1
Pillow==10.4.0

# --- Testing ---
pytest==8.2.2
pytest-mock==3.14.0
pytest-cov==5.0.0

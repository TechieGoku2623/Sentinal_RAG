# Page override — Landing (`landing/app/`)

**Overrides MASTER for marketing site only.**

## Layout
- Stat-Led hero: one large eval metric + qualifier headline (not vanity conversion stats)
- Asymmetric split: copy left, pipeline / code panel right
- One graphite band max (quickstart or API mock)

## Components to build / refine (21st.dev `/ui` targets)
- Trust metric strip (animated counters — already `AnimatedMetric`)
- Pricing table (`Pricing.tsx`) — align with workspace plans
- Feature bento or stat cards for five-layer pipeline
- FAQ accordion (clinical safety, on-prem, audit)

## Do not
- Consumer marketing fluff or fake social proof
- Inter/Roboto as display face
- Full-page gradients or glassmorphism overload

## Magic prompt examples
```
/ui healthcare SaaS stat-led hero with veridian teal accent, Space Grotesk display, eval metric 64% confidence, deep teal ink on mint paper
/ui accessible pricing table three tiers starter professional enterprise clinical protocol validation
```
